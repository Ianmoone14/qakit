{
  "name": "qakit",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish",
    "github-publish": "pnpm build && node scripts/publish-github-packages.mjs",
    "github-release": "pnpm build && node scripts/publish-github-release.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.2",
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
