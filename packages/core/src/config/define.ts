import type { QakitConfig } from '@qakit/contracts';

/** Type helper for `qakit.config.ts`. Returns the same object. */
export function defineConfig(config: QakitConfig): QakitConfig {
  return config;
}
